// import { IProfile } from "./api-response";

export interface ITasktDetails {
  title: string;
  description: string;
  deadline: string;
  points: number;
  status: number;
};

export interface TaskStatus {
  0: string;
  1: string;
  2: string;
  3: string;
};
